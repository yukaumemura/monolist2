
    <div class="example">
     <div class="panel panel-primary">  
      <div class="panel-heading">現在在庫所持数の上位10個</div>
      <table class="table table-striped table-bordered">
          <caption>当サイトで登録している被災者が現在所持している物資の多い順に表示してます。</caption>
         <tbody>
    		<tr class="danger">
    			<th>順位</th>
    			<th>個数</th>
    			<th>商品名</th>
    			<th>備考</th>
    		</tr>
    		<tr><% @items.each.with_index(1) do |item,i| %>
    			<th><%= "#{i}" %></th>
    			<th></th>
    			<td><%= image_tag(item.large_image , alt: item.title) %></td>
    			<td><%= item.title %></td>
    		</tr>
    		<% end %>
    	</tbody>
      </table>
      </div>
　</div>
